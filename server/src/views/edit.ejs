<!DOCTYPE html>
<html>
<head>
  <title>Auth projekt</title>
  <meta charset="UTF-8">
  <meta name="author" content="Lorena Martinović">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
          <div class="l-flex">
            <a class="navbar-brand" href="/">Home</a>
            <% if(username) {%>
            <a class="navbar-brand" href="/logout">Log out</a>
            <% } else {%>
            <a class="navbar-brand" href="/sign-up">Sign up</a>
            <a class="navbar-brand" href="/login">Log in</a>
            <% } %>
            <a class="navbar-brand" href="/results">Results</a>
            <a class="navbar-brand" href="/rounds">Rounds</a>
          </div>
          <div class="d-flex">
            <button class="btn btn-outline-success"><%=username%></button>
          </div>
        </div>
      </nav>

    <div class="container justify-content-center align-items-center w-30 p-5">
        <form action="/edit/<%= match['matchid'] %>" method="post">
            <h2 for="score1"><%= match['matchtimestamp'] %></h2>
            <div class="input-group mb-3">
                <div class="input-group-prepend">
                    <span class="input-group-text" id="basic-addon3"><%= match['team1'] %></span>
                </div>
                <input type="text" class="form-control" id="score1"  name="score1" aria-describedby="basic-addon3" value="<%= match['firstteamscore'] %>">
            </div>
            <div class="input-group mb-3">
                <div class="input-group-prepend">
                <span class="input-group-text" id="basic-addon3"><%= match['team2'] %></span>
                </div>
                <input type="text" class="form-control" id="score2" name="score2" aria-describedby="basic-addon3" value="<%= match['secondteamscore'] %>">
            </div>
            <button class="btn btn-outline-secondary" type="submit">EDIT</button>
        </form>
    </div>
</body>